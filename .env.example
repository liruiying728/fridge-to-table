# 所有 AI 密钥与覆盖项都写在 .env.local（本文件可复制为 .env.local；勿提交真实密钥）
# 优先级：环境变量 > config/app.json 里菜谱的默认 aiBaseUrl / aiModel（仅作兜底）

OHMYGPT_BASE_URL=https://api.ohmygpt.com/v1

# 菜谱 chat/completions
OHMYGPT_RECIPE_MODEL=gemini-3.1-flash-lite-preview
OHMYGPT_RECIPE_API_KEY=

# Node 访问 api.ohmygpt.com 时走 HTTP 代理（系统「自动代理」对 Node 默认不生效）。
# 与本机 Clash / V2Ray 等设置里的 HTTP 代理端口一致，例如：
# HTTPS_PROXY=http://127.0.0.1:7890

# 前端构建（可选）：仅当页面与 API 不同域时设置，无尾斜杠；留空则浏览器请求当前站点下的 /api（与 Express 同域部署时不必设）
# VITE_API_BASE_URL=https://your-api.onrender.com
